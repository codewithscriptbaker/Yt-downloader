# Optional HTTPS terminator example.
# Place certs at ./nginx/certs/fullchain.pem and ./nginx/certs/privkey.pem
# then include this file from nginx.conf or replace the HTTP server block.

# server {
#     listen 443 ssl http2;
#     server_name your.domain;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     # ... same location blocks as nginx.conf ...
# }
